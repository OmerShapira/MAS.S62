using UnityEngine;
using System.Collections;

public class Sidewalk : MonoBehaviour {

	// Use this for initialization
	void Start () {
			
	}
	
	// Update is called once per frame
	void Update () {
//		if (Input.GetKeyDown(KeyCode.Space)){
//			Debug.Log("Sidewalk Bounds: " + GetComponent<MeshCollider>().bounds.size);
//			Debug.Log("Sidewalk Center: " + GetComponent<MeshCollider>().bounds.center);
//		}
	}
}
